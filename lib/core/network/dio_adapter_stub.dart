import 'package:dio/dio.dart';

/// Stub for cross-platform Dio HTTP client adapter configuration
void configureDioAdapter(Dio dio) {
  throw UnsupportedError('Cannot configure Dio adapter on this platform');
}
